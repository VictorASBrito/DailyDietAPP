import { TotalTitle, DescriptionTitle } from '@components/StatisticsCard/style'
import { SnacksOnDietContainer } from './style'

export function SnacksOnDietCard() {
  return (
    <SnacksOnDietContainer>
      <TotalTitle>99</TotalTitle>
      <DescriptionTitle>Refeições dentro da dieta</DescriptionTitle>
    </SnacksOnDietContainer>
  )
}
